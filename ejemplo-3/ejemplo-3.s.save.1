
.data

variable: .asciz "%d %d \r\n"

.text
.global _start
.global main
.extern printf     # Similar al include de C

main:
_start:

    
    subq $8, %rsp  # Alineamos los datos 
    
    mov $4, %rax
    mov $5, %rbx

    mov $mensaje, %rdi
    mov 
